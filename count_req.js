const { MongoClient } = require("mongodb"); const uri = ""; async function run() { const client = new MongoClient(uri); try { await client.connect(); const db = client.db("LocalChefBazaar"); console.log("roleRequest Count:", await db.collection("roleRequest").countDocuments()); console.log("requests Count:", await db.collection("requests").countDocuments()); } finally { await client.close(); } } run();
